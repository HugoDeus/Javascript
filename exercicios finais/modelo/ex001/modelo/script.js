let num = document.getElementById('anal')
let list = document.getElementById('lista')
let res = document.getElementById('lista')

function adicionar() {
    let nume = []
    let nu = Number(nume.value)
    let n = Number(num.value)
    for (let p of nu)
    if (nu == ' ') {
        alert('Digite um númeo')
    } else {
        list.innerHTML = p
    }
}